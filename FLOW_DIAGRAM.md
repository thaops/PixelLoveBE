# 🎨 Visual Flow Diagrams

## 🔐 Complete Login & Couple Connection Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         🚀 USER JOURNEY                                  │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   📱 App     │
│   Opened     │
└──────┬───────┘
       │
       ▼
┌─────────────────────────┐
│  Click "Login Google"   │
└──────────┬──────────────┘
           │
           ▼
┌─────────────────────────────────┐
│  Google OAuth Flow              │
│  → User grants permission       │
│  → Get accessToken              │
└──────────┬──────────────────────┘
           │
           ▼
┌──────────────────────────────────────────────┐
│  POST /auth/google                           │
│  { provider, accessToken }                   │
└──────────┬───────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│  Backend verifies token with Google API        │
│  → Get: id, email, name, avatar                │
└──────────┬─────────────────────────────────────┘
           │
           ▼
     ┌─────────────┐
     │ User exists?│
     └─────┬───────┘
           │
   ┌───────┴────────┐
   NO              YES
   │               │
   ▼               ▼
┌──────────────┐  ┌───────────────────┐
│ Create User  │  │ Load User         │
│ - email      │  │ - Check profile   │
│ - avatar     │  │   complete        │
│ - mode:solo  │  │                   │
└──────┬───────┘  └──────┬────────────┘
       │                 │
       └────────┬────────┘
                │
                ▼
         ┌──────────────┐
         │ Has name &   │
         │ dob?         │
         └──────┬───────┘
                │
        ┌───────┴────────┐
        NO              YES
        │               │
  needProfile=true  needProfile=false
        │               │
        ▼               ▼
┌──────────────────┐  ┌────────────────┐
│ Response:        │  │ Response:      │
│ {                │  │ {              │
│   token,         │  │   token,       │
│   needProfile:   │  │   needProfile: │
│     true,        │  │     false,     │
│   user: {        │  │   user: {      │
│     name: null,  │  │     name: "X", │
│     dob: null    │  │     dob: "..." │
│   }              │  │   }            │
│ }                │  │ }              │
└────────┬─────────┘  └────────┬───────┘
         │                     │
         ▼                     │
┌──────────────────┐           │
│ Show Profile     │           │
│ Setup Screen     │           │
│ - Enter name     │           │
│ - Pick DOB       │           │
└────────┬─────────┘           │
         │                     │
         ▼                     │
┌──────────────────────────┐  │
│ POST /auth/update-profile│  │
│ {                        │  │
│   name: "...",           │  │
│   dob: "2000-05-15"      │  │
│ }                        │  │
└────────┬─────────────────┘  │
         │                     │
         ▼                     │
┌──────────────────────────┐  │
│ Backend calculates       │  │
│ zodiac from DOB          │  │
│ → Save to DB             │  │
└────────┬─────────────────┘  │
         │                     │
         └──────────┬──────────┘
                    │
                    ▼
            ┌───────────────┐
            │  🏠 HOME      │
            │  mode: "solo" │
            └───────┬───────┘
                    │
         ┌──────────┴───────────┐
         │                      │
    Want to create          Partner has
    couple code             couple code
         │                      │
         ▼                      ▼
┌──────────────────┐    ┌─────────────────┐
│ Click "Create    │    │ Click "Join     │
│ Couple"          │    │ Couple"         │
└────────┬─────────┘    └────────┬────────┘
         │                       │
         ▼                       ▼
┌──────────────────────┐ ┌────────────────────┐
│ POST /couple/        │ │ Enter partner's    │
│ generate-code        │ │ code               │
└────────┬─────────────┘ └────────┬───────────┘
         │                        │
         ▼                        ▼
┌──────────────────────┐ ┌───────────────────────┐
│ Response:            │ │ POST /couple/         │
│ {                    │ │ join-by-code          │
│   coupleCode:        │ │ { coupleCode: "..." } │
│     "ABC123"         │ │                       │
│ }                    │ └────────┬──────────────┘
└────────┬─────────────┘          │
         │                        │
         │  💬 Share code         │
         │  (chat, QR, etc)       │
         │                        │
         └───────────┬────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ Backend validates:     │
         │ - Code exists          │
         │ - Not own code         │
         │ - Partner available    │
         │ - User available       │
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ Create CoupleRoom     │
         │ - members: [A, B]     │
         │ - petLevel: 1         │
         └───────────┬───────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │ Update Both Users:    │
         │ User A:               │
         │ - mode: "couple"      │
         │ - partnerId: B        │
         │ - coupleRoomId: X     │
         │                       │
         │ User B:               │
         │ - mode: "couple"      │
         │ - partnerId: A        │
         │ - coupleRoomId: X     │
         │ - coupleCode: null    │
         └───────────┬───────────┘
                     │
                     ▼
            ┌────────────────┐
            │ 💑 CONNECTED!  │
            │ Both users now │
            │ in couple mode │
            └────────────────┘
```

---

## 🔄 State Transitions

```
┌─────────────────────────────────────────────────────────────┐
│                    USER STATE MACHINE                        │
└─────────────────────────────────────────────────────────────┘

[New User]
    │
    │ First Login
    ▼
[Incomplete Profile]
name: null
dob: null
zodiac: null
mode: "solo"
    │
    │ Complete Profile
    ▼
[Complete Profile - Solo]
name: "..."
dob: "..."
zodiac: "..."
mode: "solo"
    │
    │ Generate Couple Code
    ▼
[Waiting for Partner]
coupleCode: "ABC123"
mode: "solo"
    │
    │ Partner Joins
    ▼
[Connected - Couple]
coupleCode: null
partnerId: "..."
coupleRoomId: "..."
mode: "couple"
```

---

## 🗄️ Database Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE STRUCTURE                        │
└─────────────────────────────────────────────────────────────┘

         ┌──────────────────┐
         │      User A      │
         │                  │
         │ _id: "A123"      │
         │ email: "a@..."   │
         │ name: "User A"   │
         │ dob: "..."       │
         │ zodiac: "Taurus" │
         │ mode: "couple"   │───────┐
         │ partnerId: "B456"│───┐   │
         │ coupleRoomId:"R1"│─┐ │   │
         └──────────────────┘ │ │   │
                              │ │   │
         ┌────────────────────┼─┼───┼────┐
         │                    │ │   │    │
         ▼                    │ │   │    ▼
┌──────────────────┐          │ │   │  ┌──────────────────┐
│   CoupleRoom     │          │ │   │  │     User B       │
│                  │          │ │   │  │                  │
│ _id: "R1"        │◀─────────┘ │   │  │ _id: "B456"      │
│ code: "XYZ789"   │            │   │  │ email: "b@..."   │
│ members: [       │            │   └─▶│ partnerId: "A123"│
│   "A123",        │            │      │ mode: "couple"   │
│   "B456"         │            └─────▶│ coupleRoomId:"R1"│
│ ]                │                   └──────────────────┘
│ petLevel: 1      │
│ exp: 0           │
└──────────────────┘

RELATIONSHIPS:
- User A.partnerId → User B._id
- User B.partnerId → User A._id
- User A.coupleRoomId → CoupleRoom._id
- User B.coupleRoomId → CoupleRoom._id
- CoupleRoom.members contains [User A._id, User B._id]
```

---

## 🎭 Couple Matching Process

```
┌─────────────────────────────────────────────────────────────┐
│              COUPLE MATCHING SEQUENCE                        │
└─────────────────────────────────────────────────────────────┘

User A                        Backend                     User B
  │                              │                           │
  │ 1. Generate Code             │                           │
  ├─────────────────────────────▶│                           │
  │                              │                           │
  │                              │ Create coupleCode         │
  │                              │ Save to User A            │
  │                              │                           │
  │◀─────────────────────────────┤                           │
  │ { coupleCode: "ABC123" }     │                           │
  │                              │                           │
  │                              │                           │
  │  2. Share code via chat/QR   │                           │
  │──────────────────────────────┼──────────────────────────▶│
  │          "ABC123"            │                           │
  │                              │                           │
  │                              │     3. Join with code     │
  │                              │◀──────────────────────────┤
  │                              │  { coupleCode: "ABC123" } │
  │                              │                           │
  │                              │ Validate:                 │
  │                              │ - Find User A by code     │
  │                              │ - Check availability      │
  │                              │ - Create CoupleRoom       │
  │                              │ - Update both users       │
  │                              │                           │
  │                              ├──────────────────────────▶│
  │                              │ { message: "Connected!" } │
  │                              │                           │
  │ 4. Realtime notification     │                           │
  │◀─────────────────────────────┤                           │
  │ "Partner connected!"         │                           │
  │                              │                           │
  │                              │                           │
  │  5. Both users refresh       │                           │
  ├─────────────────────────────▶│                           │
  │ GET /user/profile            │                           │
  │                              │                           │
  │◀─────────────────────────────┤                           │
  │ { mode: "couple",            │                           │
  │   partnerId: "B456" }        │                           │
  │                              │◀──────────────────────────┤
  │                              │ GET /user/profile         │
  │                              │                           │
  │                              ├──────────────────────────▶│
  │                              │ { mode: "couple",         │
  │                              │   partnerId: "A123" }     │
  │                              │                           │
```

---

## 🧩 Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                  BACKEND ARCHITECTURE                        │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐
│  AuthController  │
└────────┬─────────┘
         │
         │ uses
         ▼
┌──────────────────┐        ┌─────────────────┐
│   AuthService    │───────▶│  calculateZodiac│
└────────┬─────────┘        │  (utility)      │
         │                  └─────────────────┘
         │ uses
         ▼
┌──────────────────┐
│   User Model     │
│  (MongoDB)       │
└──────────────────┘


┌──────────────────┐
│ CoupleController │
└────────┬─────────┘
         │
         │ uses
         ▼
┌──────────────────┐        ┌─────────────────┐
│  CoupleService   │───────▶│ generateCouple  │
└────────┬─────────┘        │ Code (utility)  │
         │                  └─────────────────┘
         │ uses
         ▼
    ┌────────┴────────┐
    │                 │
    ▼                 ▼
┌──────────┐  ┌──────────────┐
│   User   │  │  CoupleRoom  │
│  Model   │  │   Model      │
└──────────┘  └──────────────┘
```

---

## 📊 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│              DATA FLOW - LOGIN TO COUPLE                     │
└─────────────────────────────────────────────────────────────┘

Mobile App         Backend API        MongoDB         Google API
    │                   │                │                │
    │ 1. OAuth Token    │                │                │
    ├──────────────────▶│                │                │
    │                   │ 2. Verify      │                │
    │                   ├────────────────┼───────────────▶│
    │                   │                │                │
    │                   │◀───────────────┼────────────────┤
    │                   │ 3. User Info   │                │
    │                   │                │                │
    │                   │ 4. Find/Create │                │
    │                   ├───────────────▶│                │
    │                   │                │                │
    │                   │◀───────────────┤                │
    │                   │ 5. User Data   │                │
    │                   │                │                │
    │◀──────────────────┤                │                │
    │ 6. JWT + User     │                │                │
    │                   │                │                │
    │ 7. Update Profile │                │                │
    ├──────────────────▶│                │                │
    │                   │ 8. Calculate   │                │
    │                   │    Zodiac      │                │
    │                   │                │                │
    │                   │ 9. Update DB   │                │
    │                   ├───────────────▶│                │
    │                   │                │                │
    │◀──────────────────┤                │                │
    │ 10. Updated User  │                │                │
    │                   │                │                │
    │ 11. Generate Code │                │                │
    ├──────────────────▶│                │                │
    │                   │ 12. Save Code  │                │
    │                   ├───────────────▶│                │
    │                   │                │                │
    │◀──────────────────┤                │                │
    │ 13. coupleCode    │                │                │
    │                   │                │                │
    │ 14. Join Code     │                │                │
    ├──────────────────▶│                │                │
    │                   │ 15. Validate   │                │
    │                   │     + Link     │                │
    │                   ├───────────────▶│                │
    │                   │                │                │
    │◀──────────────────┤                │                │
    │ 16. Connected!    │                │                │
    │                   │                │                │
```

---

## 🎯 Key Decision Points

```
┌─────────────────────────────────────────────────────────────┐
│                   DECISION TREE                              │
└─────────────────────────────────────────────────────────────┘

Login
  │
  ├─ User exists? ────── NO ──▶ Create minimal user
  │                              └─▶ needProfile = true
  │
  └─ YES
      │
      └─ Has name & dob? ─── NO ──▶ needProfile = true
                             │
                             YES ─▶ needProfile = false


Generate Couple Code
  │
  ├─ Has existing code? ─── YES ──▶ Return existing code
  │
  ├─ Already in couple? ─── YES ──▶ Error: Already connected
  │
  └─ Generate new code ──────────▶ Save to user


Join Couple Code
  │
  ├─ Code exists? ────────── NO ──▶ Error: Invalid code
  │
  ├─ Is own code? ────────── YES ─▶ Error: Cannot use own code
  │
  ├─ User available? ────── NO ──▶ Error: Already connected
  │
  ├─ Partner available? ──── NO ──▶ Error: Partner connected
  │
  └─ All OK ──────────────────────▶ Create connection
                                    └─▶ Link both users
                                        Create room
                                        Set mode = couple
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
└─────────────────────────────────────────────────────────────┘

Mobile App
    │
    │ OAuth Token (from Google/Facebook)
    ▼
┌──────────────────┐
│ Backend receives │
└────────┬─────────┘
         │
         │ Layer 1: Verify OAuth Token
         ▼
┌──────────────────────┐
│ Call Google/Facebook │     ✅ Token valid?
│ API to verify        │────▶│  ├─ YES: Continue
└──────────────────────┘     │  └─ NO: 401 Unauthorized
         │
         │ Layer 2: Create/Find User
         ▼
┌──────────────────┐
│ Generate JWT     │
│ with user._id    │
└────────┬─────────┘
         │
         │ Return JWT to client
         ▼
Mobile App stores JWT
    │
    │ All future requests
    ▼
┌──────────────────┐
│ Send JWT in      │
│ Authorization    │
│ header           │
└────────┬─────────┘
         │
         │ Layer 3: JWT Verification
         ▼
┌──────────────────┐
│ JwtAuthGuard     │     ✅ JWT valid?
│ verifies token   │────▶│  ├─ YES: Extract user
└──────────────────┘     │  └─ NO: 401 Unauthorized
         │
         │ Layer 4: Business Logic Validation
         ▼
┌──────────────────┐
│ Check:           │
│ - User exists    │
│ - Not connected  │
│ - Valid input    │
└──────────────────┘
```

---

**📌 All diagrams represent the complete implemented flow!**

